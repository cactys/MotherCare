export type ComplexObject = {
  currentPage: string;
  setCurrentPage: (arg0: string) => void;
};

export type TCard = {
  id?: string;
  img: string;
  title: string;
  subtitle: string;
  copy: string;
  isModal?: boolean;
};
